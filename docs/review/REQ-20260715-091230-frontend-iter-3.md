# Independent Review — REQ-20260715-091230-frontend iteration 3

## Verdict

`PASS` for implementation `38479e5` at the machine-verifiable code gate.

The bounded live-QA port-collision class is closed: the expense app defaults to `127.0.0.1:8766`, binds exclusively, refuses an occupied requested endpoint without success output or fallback, and prints only the actual bound origin after success. This review does not perform or claim renewed live human QA and does not authorize `ACCEPTED`.

## Port And Startup Contract

- `DEFAULT_PORT`, parser default/help, frontend README, frontend root-README proposal, and product-owned root README all identify `8766`; `8765` remains reserved for the loop dashboard.
- `LocalExpenseHTTPServer.allow_reuse_address` and `allow_reuse_port` are both false.
- On Windows, `SO_EXCLUSIVEADDRUSE` is set in `server_bind()` before the superclass performs `bind()`.
- An occupied endpoint is rejected through direct `LocalExpenseHTTPServer`, `create_server`, and CLI startup. CLI returns 1 before either the success URL or database line and never enters `serve_forever` or silently chooses another port.
- Successful startup reads `server.server_address` after bind. The `--port 0` test follows the printed origin and proves that both `/` and `/api/session` are served by this expense application.
- A separate reverse probe started the expense app first, then attempted a second bind with `SO_REUSEADDR`; the second socket was rejected (`EXCLUSIVE_REBIND_REJECTED`).

## Regression And Review Lenses

- Prior blocker closure: unchanged frozen acceptance remains 6/6, covering Host/security handling and all four committed-mutation refresh paths.
- Exact behavior: frontend 13/13 and real Chromium 1/1 preserve both fixture oracles, exact BigInt reconciliation, bad-row visibility, decisions, re-import, undo, reload, accessibility, and persistence.
- Backend/invariants: backend 68/68; INV-1 through INV-8 remain intact.
- Scope creep: none. Commit `38479e5` changes only the authorized server, frontend startup docs/proposal, server regression tests, and frontend lane/evidence records. The dashboard implementation is untouched; product alone applied the matching root README text.
- Looks-done-but-wrong: none found. Occupied CLI startup exits rather than printing a plausible but wrong origin, and the printed ephemeral origin is dynamically proven reachable.
- Ease of misuse: no remaining wrong-but-accepted port path found. Explicit occupied ports fail, port 0 is opt-in and reports its actual allocation, and no automatic fallback exists.
- Privacy/local boundary: PASS. Binding remains exact IPv4 loopback, no CORS/remote asset/network-client/telemetry/OCR/raw-data logging was added, and failure output contains only the requested local endpoint plus the OS bind error.
- Dependencies: none added or installed.
- Human QA: **READY TO RENEW, NOT PERFORMED.** Product must start the app at the exact printed `8766` origin, validate root plus `/api/session`, ask the human to operate that URL, retain `REVIEWING`, and wait for explicit confirmation before `ACCEPTED`. Product must restore `max_fix_cycles` from the temporary 5 to the standing 3 in the same checkpoint as acceptance.

## Verification

- `python -m unittest tests.frontend.test_server -v`: exit 0, 9/9.
- `python -m unittest tests.acceptance.test_local_web_app_review -v`: exit 0, 6/6.
- `python -m unittest discover -s tests/frontend -p "test_*.py" -v`: exit 0, 13/13.
- `python -m unittest tests.frontend.test_browser_e2e -v`: exit 0, 1/1 on the existing Chromium runtime.
- `python -m unittest discover -s tests/backend -p "test_*.py" -v`: exit 0, 68/68.
- `python completion_gate.py --loop-dir docs/loop --request-id REQ-20260715-091230-frontend`: exit 0, `SHIP_CHECK_OK REQ-20260715-091230-frontend`.
- Reverse exclusive-rebind probe: exit 0, `EXCLUSIVE_REBIND_REJECTED`.
- `python multi_agent_loop_doctor.py --loop-dir docs/loop --json`: exit 0, `ok: true`; current request is durably `REVIEWING`, all evidence/gates are present, and the human-QA hold is recognized.

## Integrity Checks

- Frozen acceptance SHA-256: `4EB538618C7455BB857DA7F30F1341F77AE6EA3086B151E87AFA6940B4D3C7D7`.
- CSV fixture SHA-256: `9F93F107DDD02AEE2FB0A0AE19EB962180C680D1FB06D4D90959C96ED8CD7BAA`.
- PDF fixture SHA-256: `F4B6B524D7C6140EE3F3E24CE87C4A36F0B40619395B5DC17B03F1B99DBC11F8`.
- All six product-root and frontend-lane iteration-3 evidence pairs are byte-identical.

## Evidence

- `docs/loop/lanes/review/evidence/REQ-20260715-091230-frontend-iter-3-review-pass.json`
- `tests/acceptance/test_local_web_app_review.py`
