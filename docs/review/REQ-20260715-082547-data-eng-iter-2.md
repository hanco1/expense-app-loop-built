# Local JSON application facade review - iteration 2

Verdict: **PASS** for implementation commit `0362cb0`.

## Blocker closure

- **Local database boundary: closed.** `CoreStore.__init__` normalizes every
  supported text/path-like input and rejects UNC, forward-slash network-share,
  and Windows device-namespace shapes before filesystem probing or SQLite
  connection (`backend/persistence.py:420-438`). Direct `CoreStore`,
  `LocalWebApi.from_database`, and the exported `LocalExpenseApi` alias all
  share this guard. The unchanged review acceptance and expanded backend
  boundary test are green.
- **Atomic repeated undo: closed.** The strict facade route now calls
  `undo_import_run_if_active`; active-state comparison and both occurrence/run
  updates execute under the same `BEGIN IMMEDIATE` transaction
  (`backend/persistence.py:857-889`). One concurrent facade returns 200 and the
  other 409. Entity counts and correction IDs remain unchanged, while the
  existing restorative core undo entry point retains its prior behavior.
- **Mixed-currency run inspection: closed.** Run serialization reads the active
  analysis transaction projection without invoking monthly aggregation
  (`backend/local_web_api.py:422-465`). The month route remains a 409 conflict,
  while both retained run details return structured 200 responses without
  `retained_input`.
- **Accepted CSRF configuration: closed.** Explicit configured tokens are
  rejected at construction unless they are non-empty ASCII text
  (`backend/local_web_api.py:59-71`); every accepted token remains usable by
  the exact constant-time comparison. Unsupported text is never exposed by the
  session route.

## Per-criterion results

- C1: **PASS.** Session metadata is stable; every mutation enforces exact CSRF
  before writes; unsupported configured text fails early; no CORS or raw-data
  logging surface exists.
- C2: **PASS.** CSV remains 23/22/1/22, PDF remains 12/12/0/12, the failed row
  is inspectable, all run states persist newest first, and detail omits retained
  input even during mixed-currency conflict.
- C3: **PASS.** Exact re-import remains independently inspectable and
  idempotent; concurrent/repeated undo has exactly one success; facts and
  category correction history survive support loss and later re-import.
- C4: **PASS.** May and June exact oracles/category maps remain unchanged; all
  JSON money values are base-10 strings and positive credits remain
  non-spending.
- C5: **PASS.** Category and duplicate decisions append exactly once; pending,
  same, and distinct semantics remain correct; invalid component proposals
  return 409 without appending history.
- C6: **PASS.** Validation, missing, conflict, media, size, parser/import,
  mixed-currency, and unexpected-error envelopes remain stable and disclose no
  raw input, retained text, exception text, or traceback.
- C7: **PASS.** Backend discovery is 68/68; fixture hashes and dependency pin
  are unchanged; no listener, network client, OCR, subprocess, telemetry,
  system dependency, raw logging, or out-of-scope persistence was added.

## Independent assessment

- Invariants: INV-1 through INV-8 remain satisfied. In particular, strict undo
  changes state without deleting facts/history, run details remain traceable
  during aggregate conflicts, money stays exact, and supported public database
  construction fails closed on network-share path forms.
- Scope creep: none. The `3e9bd6c..0362cb0` implementation diff is limited to
  the seven declared product/test/doc artifacts plus data-eng's own lane state
  and inbox move.
- Looks-done-but-wrong: none found. The previous green-but-incomplete cases now
  have red-capable regressions at the storage transaction, public factory,
  run-detail, and constructor boundaries, and code inspection agrees with their
  observed results.
- Ease of misuse: none found within the supported public contract. Direct
  `CoreStore`, `LocalWebApi`, and the exported `LocalExpenseApi` alias share the
  same path and write boundaries; invalid state/configuration is rejected
  rather than accepted with a wrong result.
- Privacy/local boundary: pass. No raw input is logged or returned, no network
  or listener surface exists, and the approved fixture hashes are unchanged.
- Human-QA readiness: this remains a listener-free, non-UI backend slice, so no
  live human QA applies yet. It is ready to serve as the reviewed boundary for
  the later user-facing UI slice, which will require its own human-QA gate.

## Independent verification

- `python -m unittest tests.acceptance.test_local_web_api_review -v`: exit 0,
  4/4; SHA-256
  `D1E9C7E00429E234980F367ED64913143D33415198665D7EF6EB5C89E7F962E2`.
- `python -m unittest tests.backend.test_local_web_api_security -v`: exit 0,
  3/3.
- `python -m unittest tests.backend.test_local_web_api_imports -v`: exit 0,
  2/2.
- `python -m unittest tests.backend.test_local_web_api_runs -v`: exit 0, 2/2.
- `python -m unittest tests.backend.test_local_web_api_months -v`: exit 0, 1/1.
- `python -m unittest tests.backend.test_local_web_api_decisions -v`: exit 0,
  2/2.
- `python -m unittest tests.backend.test_local_web_api_errors -v`: exit 0, 3/3.
- `python -m unittest tests.backend.test_local_web_api_boundary -v`: exit 0,
  3/3.
- `python -m unittest discover -s tests/backend -p "test_*.py" -v`: exit 0,
  68/68.
- `python completion_gate.py --loop-dir docs/loop --request-id REQ-20260715-082547-data-eng`:
  exit 0, `SHIP_CHECK_OK REQ-20260715-082547-data-eng`.
- Loop doctor: exit 0, `ok: true`, no active-request issue, evidence manifest
  gap, scope overlap, or uncommitted-work warning; historical warnings only.
- Fixture SHA-256 values independently remain CSV
  `9F93F107DDD02AEE2FB0A0AE19EB962180C680D1FB06D4D90959C96ED8CD7BAA`
  and PDF
  `F4B6B524D7C6140EE3F3E24CE87C4A36F0B40619395B5DC17B03F1B99DBC11F8`.

Structured review evidence:
`docs/loop/lanes/review/evidence/REQ-20260715-082547-data-eng-iter-2-review-pass.json`.
