# Independent Review — REQ-20260715-091230-frontend iteration 2

## Verdict

`PASS` for implementation `8131762` at the machine-verifiable code gate.

Both iteration-1 blocker classes are closed without changing the frozen review acceptance. This verdict does not perform or claim live human QA and does not authorize `ACCEPTED`; product must keep the user-facing request at `REVIEWING` while it runs the separate human-QA gate.

## Blocker Closure

### B1 — Public HTTP methods now share the Host-first structured boundary

- `LocalExpenseRequestHandler.__getattr__` routes every otherwise-unimplemented `do_*` method to `_method_not_allowed`.
- Invalid Host requests return structured `421 invalid_host` before dispatch.
- Valid loopback unsupported methods return structured `405 method_not_allowed`.
- Frozen coverage proves PATCH, TRACE, CONNECT, and PROPFIND across invalid/valid Host; frontend coverage adds OPTIONS. Every response carries the complete security-header set and no CORS allowance. No default HTML 501 path remains reachable through a parsed public method.

### B2 — A committed mutation is no longer relabelled as failed

- Import, category correction, duplicate decision, and undo now catch mutation failures before establishing a commit acknowledgement.
- After a 2xx response, each path records an accurate committed message and uses `refreshAfterCommit` for the separate canonical read phase.
- A later read failure explicitly says the write is committed and the view is stale, preserves the success state, recommends reload, and performs no automatic retry.
- Frozen browser acceptance interrupts the first post-commit read, then reloads and proves one persisted run, one category history row, one duplicate history row, and the undone run state.

## Per-Criterion Result

- C1: **PASS** — exact loopback binding, same-origin routing, Host-first handling for every parsed public method, complete security headers, CSRF, no CORS, and no raw-data/token logging are green.
- C2: **PASS** — picker/drop, sequential runs, exact fixture counts, visible `missing_amount`, persistent run detail, confirmation, undo refresh, stale conflict, and committed-import/undo acknowledgement are green.
- C3: **PASS** — newest June default, exact May/June oracles, BigInt-only money/chart calculations, full reconciliation, credit exclusion, and selectable non-zero slices remain green.
- C4: **PASS** — category and Tim Hortons decision flows remain append-only and persistent; 409 remains non-optimistic; committed category/duplicate writes remain accurately acknowledged across refresh failure.
- C5: **PASS** — semantic keyboard/focus/live-region behavior, narrow layout, safe text rendering, distinct failure states, double-submit prevention, and post-commit stale-view messaging are green.
- C6: **PASS** — independent real Chromium execution completed the approved two-fixture import, exact oracle, bad row, correction, duplicate decision, re-import, undo, reload, and persistence flow.
- C7: **PASS** — frozen acceptance 6/6, frontend 9/9, Chromium 1/1, backend 68/68, fixture hashes, startup/shutdown, dependencies, privacy boundary, and `SHIP_CHECK_OK` are green.

## Standing Review Lenses

- Invariants: INV-1 through INV-8 remain intact. Persisted facts, supports, corrections, duplicate history, exact amounts, and source traceability are unchanged.
- Scope creep: none. The implementation changes only the authorized server/UI files, focused frontend regressions, and frontend lane/evidence records; no dependency, backend rule, OCR, network-client, telemetry, deployment, or system-runtime surface changed.
- Looks-done-but-wrong: none found after the frozen post-commit failure probes and full fixture flow.
- Ease of misuse: no remaining wrong-but-accepted caller/input path found. Unsupported methods cannot bypass Host/security handling, and refresh failure no longer invites retry by denying a committed write.
- Privacy/local boundary: PASS. Browser requests remain same-origin relative paths; no remote asset, CORS, telemetry, raw statement logging, token logging, or non-loopback listener path was added.
- Human QA: **READY TO REQUEST, NOT PERFORMED.** Product must provide the live local URL and 30-second operating check, retain `REVIEWING`, and wait for explicit human confirmation before `ACCEPTED`.

## Verification

- `python -m unittest tests.acceptance.test_local_web_app_review -v`: exit 0, 6/6.
- `python -m unittest discover -s tests/frontend -p "test_*.py" -v`: exit 0, 9/9.
- `python -m unittest tests.frontend.test_browser_e2e -v`: exit 0, 1/1 on the existing Chromium runtime.
- `python -m unittest discover -s tests/backend -p "test_*.py" -v`: exit 0, 68/68.
- `python completion_gate.py --loop-dir docs/loop --request-id REQ-20260715-091230-frontend`: exit 0, `SHIP_CHECK_OK REQ-20260715-091230-frontend`.
- `python multi_agent_loop_doctor.py --loop-dir docs/loop --json`: exit 0, `ok: true`, no request owner, evidence, scope, gate, or stalled-handoff issue.

## Integrity Checks

- Frozen acceptance SHA-256: `4EB538618C7455BB857DA7F30F1341F77AE6EA3086B151E87AFA6940B4D3C7D7`.
- CSV fixture SHA-256: `9F93F107DDD02AEE2FB0A0AE19EB962180C680D1FB06D4D90959C96ED8CD7BAA`.
- PDF fixture SHA-256: `F4B6B524D7C6140EE3F3E24CE87C4A36F0B40619395B5DC17B03F1B99DBC11F8`.
- All five product-root and frontend-lane iteration-2 evidence pairs are byte-identical.

## Evidence

- `docs/loop/lanes/review/evidence/REQ-20260715-091230-frontend-iter-2-review-pass.json`
- `tests/acceptance/test_local_web_app_review.py`
