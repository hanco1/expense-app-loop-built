# Statement import pipeline review - iteration 2

Verdict: **FAIL - blocker confirmed; anti-thrash escalation required**.

## Finding

- **Blocker: Decimal exceptions still drop the source row instead of retaining
  `invalid_amount`.** `_parse_cents` catches `InvalidOperation` only while
  constructing `Decimal` (`backend/statement_import.py:271-274`). `NaN` raises
  `InvalidOperation` later at the comparison on line 277, and `1e999999`
  raises `decimal.Overflow` during multiplication on line 275. Both exceptions
  escape the row-level `RecordParseFailure` handler, so `import_bytes` returns
  `StatementImportFailure` and a terminal failed run whose summary is 0 source
  records / 0 parsed / 0 failed. The candidate CSV row is absent rather than
  retained with `invalid_amount`, violating the iteration-2 acceptance
  criterion and INV-1.

## Three-category review

- Unmet/partial criteria: ordinary oversized fixed-point amounts now become
  explicit `invalid_amount` rows, but Decimal exceptional values do not.
- Scope creep: none; commit `6bca89e` changes only declared backend/test files
  and protocol-mandated data-eng lane records.
- Looks-done-but-wrong: all eight declared commands and `SHIP_CHECK_OK` are
  green, but their oversized-amount example never triggers Decimal arithmetic
  exceptions.

Ease of misuse: a caller can submit a header-valid five-column CSV containing
`NaN` or `1e999999` in debit/credit. The import fails safely with no effective
support, but silently loses the candidate source row from run detail instead of
recording the required explicit failure.

## Passing coverage

- The iteration-1 partial-active-state blocker is closed for tested persistence
  failures: failed runs are inspectable, terminal, non-effective, occurrences
  are excluded with `import_failed`, and incomplete parsed rows report
  `persistence_incomplete`.
- Approved fixture hashes/fields, CSV 23/22/1, PDF 12/12/0, 34 effective
  identities, exact re-import, Tim Hortons suspected-pair visibility, undo,
  correction survival, provenance, privacy, and non-goals remain green.

## Anti-thrash decision

The live doctor reconstructs `fix_cycles=3` and `loop-policy.md` sets
`max_fix_cycles: 3`. Protocol therefore forbids another automatic
`FIX_REQUESTED -> IMPLEMENTING` round. The request is escalated `BLOCKED` to
product/human rather than incorrectly passed or bounced to iteration 3.

## Evidence

- All eight declared iteration-2 commands: exit 0; backend discovery 26/26 and
  the original review acceptance suite 2/2.
- `python -m unittest tests.acceptance.test_statement_import_review -v` after
  adding Decimal-exception coverage: exit 1; two subtest errors at
  `tests/acceptance/test_statement_import_review.py:131`.
- Completion gate: exit 0, `SHIP_CHECK_OK REQ-20260713-073512-data-eng`.
- Doctor: exit 0, `fix_cycles=3`, `max_fix_cycles=3`.
- Structured review evidence:
  `docs/loop/lanes/review/evidence/REQ-20260713-073512-data-eng-iter-2-review.json`.
