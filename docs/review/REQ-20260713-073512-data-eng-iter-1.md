# Statement import pipeline review - iteration 1

Verdict: **FAIL - blocker fix required**.

## Finding

- **Blocker: a rejected CSV import leaves a partial active run and an effective
  transaction.** `StatementImportService.import_bytes` commits the run before
  parsing and persists each source record, identity, normalized transaction,
  duplicate link, and occurrence through separate store transactions
  (`backend/statement_import.py:163-201`). A supported-shape CSV containing one
  ordinary row followed by an amount outside SQLite's signed-integer range
  fails at `backend/persistence.py:448`, but the first row remains included and
  effective. The caller receives an exception instead of the `run_id`, while
  the second retained row is reported as `parsed` with no occurrence and the
  nonsensical inclusion reason `parse_failed:None`
  (`backend/persistence.py:716-721`). This violates INV-1, INV-4, and INV-6.

## Three-category review

- Unmet/partial criteria: approved fixture behavior, undo, re-import, and
  provenance pass, but an input-level storage failure does not remain an
  explicit failed record or roll back as one import unit.
- Scope creep: none; changed files are covered by the request scope or the
  protocol-mandated data-eng lane records.
- Looks-done-but-wrong: all seven declared commands and the completion gate are
  green, yet none injects a failure after an earlier row has committed.

Ease of misuse: bytes for a header-valid CSV can place an oversized decimal in
a later debit/credit cell. `import_bytes` raises, but earlier rows become
effective under an active run whose ID was never returned to the caller.

## Passing coverage

- Fixture hashes independently recomputed to the two pinned values.
- CSV 23/22/1, PDF 12/12/0, all 34 normalized fixture field tuples, signed CAD
  cents, exact re-import identity, the visible included Tim Hortons suspected
  pair, active-support undo, correction survival, and provenance all pass.
- Source inspection found no parsing/categorization/HTTP/aggregation/UI/cloud
  scope creep and no network, OCR, subprocess, telemetry, or raw-content log
  path.

## Evidence

- All seven declared verification commands: exit 0.
- Completion gate: exit 0, `SHIP_CHECK_OK REQ-20260713-073512-data-eng`.
- `python -m unittest tests.acceptance.test_statement_import_review -v`:
  exit 1; exact fixture-field test passes and partial-import safety test fails
  at `tests/acceptance/test_statement_import_review.py:92`.
- Structured review evidence:
  `docs/loop/lanes/review/evidence/REQ-20260713-073512-data-eng-iter-1-review.json`.
