# Analysis core review - iteration 2

Verdict: **FAIL - blocker** for implementation commit `6b9378f`.

## Finding

- **Blocker: undoing only the designated keeper's support can still remove
  every active representative.** `set_duplicate_decision` validates the active
  component only when a decision is appended (`backend/analysis.py:160-214`).
  Later support changes do not repeat or enforce that invariant, while
  `_duplicate_exclusions` continues to exclude each pair's non-kept identity
  without considering which identity is currently active
  (`backend/analysis.py:419-431`). Import two different source blobs containing
  the same normalized transaction, mark the pair `same_transaction` with the
  first identity kept, then undo only the kept identity's run. The other
  identity remains in `CoreStore.list_effective_transactions()`, but both sides
  report `included=false` and `AnalysisService.list_months()` returns `()`.
  This violates C3's requirement that an active connected component cannot
  lose every representative and creates silent analytical loss under INV-1.

The consolidated review test is
`tests/acceptance/test_analysis_core_review.py:69-148`. Its assertion at line
119 receives `()`, not `("2026-06",)`. The original three-link cycle test is
green, proving the iteration-1 defect was narrowed but not closed across the
required undo/re-import state transitions.

## Per-criterion results

- C1 automatic categories and credit semantics: **PASS** (3/3).
- C2 append-only category corrections and undo/re-import survival: **PASS**
  (1/1).
- C3 duplicate-component safety: **FAIL (blocker)**. Cycle closure is rejected
  before append and the isolated-pair history remains intact, but selective
  keeper-support undo leaves a still-active non-kept identity excluded and the
  component with zero representatives.
- C4 exact May/June totals and category reconciliation: **PASS** (1/1) for the
  approved fixture state.
- C5 declared import/idempotency/inclusion cases: **PASS** (3/3). The blocker
  is the interaction between a valid duplicate decision and a later selective
  run undo, not the covered final-support undo case.
- C6 typed deterministic traceable contracts: **PASS** (2/2) outside the C3
  state; the missing month is the observable consequence of the blocker.
- C7 privacy, local boundary, and regressions: **PASS**. Local-boundary tests
  pass 3/3, backend discovery passes 46/46, fixture hashes and dependencies are
  unchanged, and no network/OCR/telemetry/subprocess surface was added.

## Invariant and three-category review

- Applicable invariants: INV-2 through INV-8 have no new independent failure.
  INV-1's no-silent-loss intent is violated in analysis because an identity
  with active source support vanishes from every monthly result.
- Unmet or partial criteria: C3 only, as described above.
- Scope creep: none. Commit `6b9378f` changes only `backend/analysis.py`,
  `tests/backend/test_analysis_duplicates.py`, `docs/data/analysis-core.md`,
  and the protocol-exempt data-eng current-state file.
- Looks-done-but-wrong: all ten delivered commands are green, but the new
  backend test undoes the one run that supports all three identities together.
  It cannot go red when the keeper and non-keeper are supported by different
  runs and only the keeper's run is undone.

Ease of misuse: **confirmed.** A caller uses only normal import, duplicate
decision, and run-undo APIs: import the same transaction from two distinct
sources, keep one identity, then undo that identity's run. The remaining active
fact is accepted by storage but excluded from analysis.

Human-QA readiness: not applicable; this remains a non-user-facing core slice.

## Independent verification

Before expanding the review acceptance, all ten delivered commands were
reproduced:

- `python -m unittest tests.acceptance.test_analysis_core_review -v`: exit 0,
  1/1.
- `python -m unittest tests.backend.test_analysis_categories -v`: exit 0, 3/3.
- `python -m unittest tests.backend.test_analysis_corrections -v`: exit 0, 1/1.
- `python -m unittest tests.backend.test_analysis_duplicates -v`: exit 0, 2/2.
- `python -m unittest tests.backend.test_analysis_monthly -v`: exit 0, 1/1.
- `python -m unittest tests.backend.test_analysis_inclusion -v`: exit 0, 3/3.
- `python -m unittest tests.backend.test_analysis_contracts -v`: exit 0, 2/2.
- `python -m unittest tests.backend.test_analysis_local_boundary -v`: exit 0,
  3/3.
- `python -m unittest discover -s tests/backend -p "test_*.py" -v`: exit 0,
  46/46.
- `python completion_gate.py --loop-dir docs/loop --request-id REQ-20260714-064051-data-eng`:
  exit 0, `SHIP_CHECK_OK REQ-20260714-064051-data-eng`.

After adding the selective-support transition to the review-owned acceptance:

- `python -m unittest tests.acceptance.test_analysis_core_review -v`: exit 1,
  2 tests, 1 failure at line 119; one non-kept identity remains active while
  `list_months()` is empty.

Structured review evidence:
`docs/loop/lanes/review/evidence/REQ-20260714-064051-data-eng-iter-2-review.json`.

## Control-plane result

The live doctor reports raw fix-cycle count 3 with `max_fix_cycles: 3`. A new
`FIX_REQUESTED` plus its required `IMPLEMENTING` claim would raise the raw count
to 5. Review therefore does not dispatch iteration 3. The request is returned
to product as `BLOCKED`; the recommended human decision is a temporary cap of
5 for one bounded iteration-3 round, restored to 3 after acceptance.
