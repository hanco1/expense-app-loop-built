# Independent Review — REQ-20260715-091230-frontend iteration 4

## Verdict

`BLOCKED` for implementation `4b88b11`.

The approved June oracle and all seven declared gates are green, but the
renderer still violates the standing product contract that every non-zero
category remain visibly represented. A supported exact allocation of one
accounting unit (`Tiny=1`, `Large=999999999`) renders both non-zero SVG paths
with browser length zero and no visible ring samples. This review did not
perform or claim live human QA and does not authorize `ACCEPTED`.

## Blocker

- `frontend/static/app.js:129-151` rounds both arc endpoints to six decimal
  places before writing the SVG path. For a one-unit slice, the start and end
  collapse to the same coordinate. Its complementary `999999999`-unit
  large-arc path has the same collapsed endpoint pair and also becomes empty.
- The exact allocation is already supported and guarded at
  `tests/frontend/test_ui_browser.py:68-82`; it is not malformed input. The
  new real-Chromium review test drives the actual monthly render with that
  valid exact response.
- Observed snapshot: units `{'Tiny': '1', 'Large': '999999999'}`; SVG lengths
  `{'Tiny': 0, 'Large': 0}`; 720-point visible counts
  `{'Tiny': 0, 'Large': 0}`. Keyboard and legend selection both still report
  true, making this a looks-done-but-wrong path rather than a missing-control
  path.

## Criteria And Review Lenses

- Frozen PG-01 through PG-06 for the approved June fixture: PASS, 1/1 test,
  including exact 1e9 units, 720 painted-ring samples, nine contiguous arcs,
  Housing dominance, tiny fixture categories, focus, Enter, and legend match.
- Standing exact-dashboard contract for every non-zero category: FAIL for the
  valid one-unit allocation above. No visible arc remains for either bucket.
- Prior server/Host/CSRF/startup and committed-mutation refresh closures: PASS.
- Original frontend real-browser flow: PASS; frontend discovery 13/13 and
  standalone Chromium E2E 1/1.
- Backend/invariants/privacy: PASS; backend 68/68, fixture hashes unchanged,
  and no listener, dependency, network client, CORS, OCR, telemetry,
  subprocess, or raw-data logging change was found.
- Scope creep: none. Implementation `4b88b11` changes only `app.js`, the
  frontend E2E regression, and frontend-lane evidence.
- Looks-done-but-wrong: present. Exact data units, DOM controls, focus, and
  selected legend state all succeed while the painted chart is empty.
- Ease of misuse: high and accepted by existing public browser logic. Any
  reconciled month whose smallest exact category receives the supported
  one-unit minimum can trigger endpoint collapse; the near-full complement
  can collapse at the same time.
- Human QA: NOT PERFORMED. Product retains that hold and final acceptance.

## Verification

- `python -m unittest tests.acceptance.test_local_web_app_pie_geometry_review -v`: exit 0, 1/1.
- `python -m unittest tests.frontend.test_server -v`: exit 0, 9/9.
- `python -m unittest tests.acceptance.test_local_web_app_review -v`: exit 0, 6/6.
- `python -m unittest discover -s tests/frontend -p "test_*.py" -v`: exit 0, 13/13.
- `python -m unittest tests.frontend.test_browser_e2e -v`: exit 0, 1/1 on the existing Chromium runtime.
- `python -m unittest discover -s tests/backend -p "test_*.py" -v`: exit 0, 68/68.
- `python completion_gate.py --loop-dir docs/loop --request-id REQ-20260715-091230-frontend`: exit 0, `SHIP_CHECK_OK REQ-20260715-091230-frontend`.
- `python -m unittest tests.acceptance.test_local_web_app_tiny_slice_review -v`: exit 1, 1 failure with the zero-length/zero-visible snapshot above.

## Integrity And Evidence

- Frozen geometry acceptance SHA-256: `D5EEA84080B915C6C1978A20D47C61EE06FD96DFF65E6ED46A822BD33A076912`.
- Frozen boundary SHA-256: `EA19310EDC2DE2DA13AD447872E569BB845F410B74E9AA21EDE54C23A2F2C4D0`.
- Prior frozen acceptance SHA-256: `4EB538618C7455BB857DA7F30F1341F77AE6EA3086B151E87AFA6940B4D3C7D7`.
- New independent acceptance SHA-256: `0A108B0EED3264FB90C31B77B8A7700B333F5CAEAB6A934FDFA3BC0C0CDBA5C1`.
- CSV fixture SHA-256: `9F93F107DDD02AEE2FB0A0AE19EB962180C680D1FB06D4D90959C96ED8CD7BAA`.
- PDF fixture SHA-256: `F4B6B524D7C6140EE3F3E24CE87C4A36F0B40619395B5DC17B03F1B99DBC11F8`.
- All seven product-root/frontend-lane iteration-4 evidence pairs are byte-identical.
- Structured review evidence:
  `docs/loop/lanes/review/evidence/REQ-20260715-091230-frontend-iter-4-review-blocked.json`.

## Policy Disposition

Raw fix-cycle use is already `7/7`. Review therefore does not dispatch an
iteration 5 or change `max_fix_cycles`. Product must keep the request blocked
and ask the human whether to authorize one narrowly bounded additional repair.
The standing cap remains restored to 3 only in the same checkpoint as a later
explicit human PASS and `ACCEPTED`.
