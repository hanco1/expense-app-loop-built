# Independent review - REQ-20260714-064051-data-eng iteration 4

Verdict: **PASS**. No blocker findings.

Implementation `9268a5e` closes the complete frozen public-write boundary
class without changing either frozen matrix. Product retains final acceptance
authority and must restore the temporary `max_fix_cycles: 7` override to the
standing value `3` in the same checkpoint that records `ACCEPTED`.

## Frozen matrices and hashes

- `tests/acceptance/test_analysis_core_write_boundary_review.py`:
  `974432935CBB3C8EB957297DA50E3FD52E1F877D1C5D9C3BBAFD4D16818D809B`.
- `docs/review/REQ-20260714-064051-data-eng-iter-4-write-boundary-matrix.md`:
  `91B1DFD3792A04DBD42E2847544896EB239594C7D9F7C2BAEAC1E4A26FCD421C`.
- `tests/acceptance/test_analysis_core_review.py`:
  `01A02F973E884D3BA917FDF73260E6E5524F9D21B8D5DE77CDD57755989C6E6B`.
- `docs/review/REQ-20260714-064051-data-eng-iter-3-component-state-matrix.md`:
  `ADB423E55F10058A556F551EECCC92CFCC998DFFC87D6FD003CF856B68FF61FB`.

All four values match the frozen contract. The iteration-4 suite passed all
144 executable paths plus its manifest guard (145/145); its six explicitly
unreachable classes remain unchanged. The 48 formerly red
`CP|CM|CA|CT x ZK|MC|MM|AD x A|P|I` paths now reject atomically. All 96
service/valid-proposal guardrails remain green. The iteration-3 suite also
passed all 55 executable paths plus its manifest guard (56/56).

## Per-criterion result

- Public writer coverage: PASS. The package, persistence, analysis-module, and
  statement-import `CoreStore` imports resolve to the same class and therefore
  the same `add_duplicate_decision()` boundary. Both `AnalysisService` imports
  delegate to that method.
- Atomic rejection: PASS. `backend/persistence.py:1067-1142` opens one
  `BEGIN IMMEDIATE` transaction, reads and overlays the latest effective graph,
  validates at `backend/persistence.py:1115-1127`, and only then allocates an ID
  and inserts at `backend/persistence.py:1129-1141`. Rejected proposals leave
  decision counts/IDs, the prior readable graph, monthly analysis, retained
  facts, supports, and structured provenance unchanged in all three support
  states.
- Graph semantics: PASS. The shared projection at
  `backend/persistence.py:40-121` uses only latest effective
  `same_transaction` edges, requires one structural keeper per component,
  rejects alternate-path `distinct`, selects the active keeper or the
  lexicographically smallest active fallback, and includes zero identities in
  an inactive component. Analysis reads reuse this projection and service
  writes delegate at `backend/analysis.py:163-174`.
- Append-only history and facts: PASS. Invalid proposals append zero history
  rows; valid VS/BD/KR/LR proposals append exactly one row and preserve prior
  IDs. Component/support changes do not rewrite decisions or delete facts.
- C1-C7 and invariants: PASS. Exact categories/corrections, duplicate history,
  May/June totals, support undo/re-import behavior, typed contracts,
  traceability, and INV-1 through INV-8 remain green. Backend discovery is
  52/52.
- Privacy/local boundary: PASS. No dependency, system package, HTTP/network,
  OCR, subprocess, telemetry, raw-content logging, UI, or fixture-hash change
  was introduced.

## Independent risk assessment

- Scope creep: none. Commit `9268a5e` changes only the declared backend,
  backend-test, data documentation, and data-eng lane paths.
- Looks-done-but-wrong: none found. The same frozen test that produced 48 red
  public-store paths on `53e57f6` now passes all 145 tests, while the original
  component matrix remains byte-for-byte unchanged and green.
- Ease of misuse: none found within the supported public Python API. Every
  exposed writer alias reaches the same transactional validator; bypass now
  requires deliberately using private/raw SQLite access outside the supported
  contract.
- Human QA: not applicable to this backend-only slice.

## Exact independent command results

| Command | Exit | Result |
| --- | ---: | --- |
| `python -m unittest tests.acceptance.test_analysis_core_write_boundary_review -v` | 0 | 145/145 |
| `python -m unittest tests.acceptance.test_analysis_core_review -v` | 0 | 56/56 |
| `python -m unittest tests.backend.test_analysis_categories -v` | 0 | 3/3 |
| `python -m unittest tests.backend.test_analysis_corrections -v` | 0 | 1/1 |
| `python -m unittest tests.backend.test_analysis_duplicates -v` | 0 | 6/6 |
| `python -m unittest tests.backend.test_analysis_monthly -v` | 0 | 1/1 |
| `python -m unittest tests.backend.test_analysis_inclusion -v` | 0 | 5/5 |
| `python -m unittest tests.backend.test_analysis_contracts -v` | 0 | 2/2 |
| `python -m unittest tests.backend.test_analysis_local_boundary -v` | 0 | 3/3 |
| `python -m unittest discover -s tests/backend -p "test_*.py" -v` | 0 | 52/52 |
| `python completion_gate.py --loop-dir docs/loop --request-id REQ-20260714-064051-data-eng` | 0 | `SHIP_CHECK_OK REQ-20260714-064051-data-eng` |

Structured evidence:
`docs/loop/lanes/review/evidence/REQ-20260714-064051-data-eng-iter-4-review-pass.json`.
